name: Build and Deploy Gym Tracker

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: [self-hosted, linux]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run build

      - name: Build Docker image
        run: docker build -t gym-tracker:latest .

      - name: Stop existing container (if any)
        run: docker rm -f gym-tracker || true

      - name: Run new container
        run: |
          docker run -d --restart unless-stopped \
            --name gym-tracker \
            -p 8004:8004 \
            gym-tracker:latest
